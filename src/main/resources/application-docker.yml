spring:
  datasource:
    url: jdbc:mysql://mysql:3306/cross-mall?serverTimezone=UTC&characterEncoding=utf8&useUnicode=true&useSSL=false&allowMultiQueries=true
    username: root
    password: ${MYSQL_ROOT_PASSWORD:123456}
  data:
    redis:
      host: redis
      port: 6379
